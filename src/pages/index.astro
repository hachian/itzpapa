---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../site.config';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import itzpapaImage from '../assets/itzpapa.png';

// 最新ブログ記事を取得
const allPosts = await getCollection('blog');
const latestPosts = allPosts
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 6);
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main style="view-transition-name: main-content;">
			<!-- ヒーローセクション -->
			<section class="hero hero--animated" aria-labelledby="site-title">
				<div class="hero__container">
					<div class="hero__banner">
						<Image
							src={itzpapaImage}
							alt="itzpapa - 紫の蝶とロータスフラワーのブランドロゴ"
							class="hero__image"
							loading="eager"
							width={150}
							height={150}
						/>
					</div>
					<div class="hero__text">
						<h1 id="site-title" class="hero__title">{SITE_TITLE}</h1>
						<p class="hero__description" set:html={SITE_DESCRIPTION}></p>
					</div>
				</div>
			</section>

			<!-- ナビゲーションセクション -->
			<section class="nav-section" aria-labelledby="nav-title">
				<div class="nav-section__container">
					<h2 id="nav-title" class="section-title">コンテンツを探索する</h2>
					<nav class="card-grid card-grid--2" role="navigation" aria-label="主要コンテンツナビゲーション">
						<a href="/blog" class="card card--nav card--link" aria-describedby="blog-desc">
							<span class="card__icon">📝</span>
							<h3 class="card__title">ブログ記事</h3>
							<p id="blog-desc" class="card__description">すべての記事を一覧で見る。カテゴリや投稿日から探せます。</p>
						</a>
						<a href="/tags" class="card card--nav card--link" aria-describedby="tags-desc">
							<span class="card__icon">🏷️</span>
							<h3 class="card__title">タグ一覧</h3>
							<p id="tags-desc" class="card__description">タグから記事を探す。興味のあるトピックを見つけましょう。</p>
						</a>
					</nav>
				</div>
			</section>

			<!-- 最新記事セクション -->
			{latestPosts.length > 0 && (
				<section class="posts-section" aria-labelledby="latest-posts-title">
					<div class="posts-section__container">
						<h2 id="latest-posts-title" class="section-title">最新の記事</h2>
						<div class="card-grid card-grid--3" role="feed" aria-label="最新ブログ記事一覧">
							{latestPosts.map((post, index) => (
								<article class="card card--link" aria-setsize={latestPosts.length} aria-posinset={index + 1}>
									<a href={`/blog/${post.id}/`} class="post-card" aria-label={`${post.data.title} - ${post.data.description}`}>
										{post.data.heroImage && (
											<Image
												src={post.data.heroImage}
												alt=""
												class="post-card__thumbnail"
												width={400}
												height={225}
											/>
										)}
										<div class="post-card__content">
											<h3 class="post-card__title">{post.data.title}</h3>
											<p class="post-card__description">{post.data.description}</p>
											<time class="post-card__date" datetime={post.data.pubDate.toISOString()}>
												{post.data.pubDate.toLocaleDateString('ja-JP', {
													year: 'numeric',
													month: 'long',
													day: 'numeric'
												})}
											</time>
										</div>
									</a>
								</article>
							))}
						</div>
						<div class="posts-section__action">
							<a href="/blog" class="btn btn--primary" aria-label="すべてのブログ記事を見る">
								すべての記事を見る
							</a>
						</div>
					</div>
				</section>
			)}
		</main>
		<Footer />
	</body>
</html>

<style>
	/* ヒーローセクション フェードインアニメーション */
	@keyframes hero-fade-in {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.hero--animated {
		animation: hero-fade-in var(--duration-slow) var(--easing-out) both;
		animation-delay: 100ms; /* LCP測定後に開始 */
	}

	/* prefers-reduced-motion でアニメーション無効化 */
	@media (prefers-reduced-motion: reduce) {
		.hero--animated {
			animation: none;
		}
	}

	/* ヒーローセクション */
	.hero {
		padding: var(--space-12) var(--space-4);
		text-align: center;
		background: linear-gradient(
			135deg,
			var(--color-primary-50) 0%,
			var(--color-primary-100) 100%
		);
		border-radius: var(--radius-2xl);
		margin: var(--space-4);
	}

	.hero__container {
		max-width: 800px;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--space-8);
	}

	.hero__banner {
		flex-shrink: 0;
	}

	.hero__image {
		width: 100px;
		height: 100px;
		object-fit: contain;
		border-radius: var(--radius-xl);
		box-shadow: var(--shadow-lg);
		transition:
			transform var(--duration-normal) var(--easing-default),
			box-shadow var(--duration-normal) var(--easing-default);
	}

	.hero__image:hover {
		transform: scale(1.08);
		box-shadow: var(--shadow-xl);
	}

	.hero__text {
		max-width: 600px;
	}

	.hero__title {
		font-size: var(--font-size-4xl);
		font-weight: var(--font-weight-bold);
		color: var(--color-primary-500);
		margin: 0 0 var(--space-4) 0;
		line-height: var(--line-height-tight);
	}

	.hero__description {
		font-size: var(--font-size-lg);
		color: var(--color-text-secondary);
		line-height: var(--line-height-relaxed);
		margin: 0;
	}

	/* セクション共通 */
	.section-title {
		text-align: center;
		font-size: var(--font-size-3xl);
		color: var(--color-text-primary);
		margin: 0 0 var(--space-10) 0;
	}

	/* ナビゲーションセクション */
	.nav-section {
		padding: var(--space-16) var(--space-4);
		background: var(--color-surface);
		border-radius: var(--radius-2xl);
		margin: var(--space-4);
		box-shadow: var(--shadow-sm);
	}

	.nav-section__container {
		max-width: 1000px;
		margin: 0 auto;
	}

	/* カードグリッド調整 */
	.card-grid--2 {
		max-width: 700px;
		margin: 0 auto;
	}

	/* ナビカード追加スタイル */
	.card--nav .card__icon {
		font-size: var(--font-size-4xl);
		margin-bottom: var(--space-4);
		display: block;
	}

	.card--nav .card__title {
		color: var(--color-primary-500);
	}

	/* 最新記事セクション */
	.posts-section {
		padding: var(--space-16) var(--space-4);
		background: var(--color-gray-50);
		border-radius: var(--radius-2xl);
		margin: var(--space-4);
		box-shadow: var(--shadow-sm);
	}

	.posts-section__container {
		max-width: 1000px;
		margin: 0 auto;
	}

	.posts-section__action {
		text-align: center;
		margin-top: var(--space-10);
	}

	/* 記事カード */
	.card-grid--3 .card {
		display: flex;
		flex-direction: column;
		padding: 0;
		overflow: hidden;
	}

	.post-card {
		display: flex;
		flex-direction: column;
		flex: 1;
		text-decoration: none;
		color: inherit;
	}

	.post-card__thumbnail {
		width: 100%;
		aspect-ratio: 16 / 9;
		object-fit: cover;
		border-radius: var(--radius-xl) var(--radius-xl) 0 0;
	}

	.post-card__content {
		display: flex;
		flex-direction: column;
		flex-grow: 1;
		padding: var(--space-5);
	}

	.post-card__title {
		font-size: var(--font-size-xl);
		font-weight: var(--font-weight-semibold);
		color: var(--color-text-primary);
		margin: 0 0 var(--space-2) 0;
		line-height: var(--line-height-snug);
		/* 2行分の固定高さ */
		height: calc(var(--font-size-xl) * var(--line-height-snug) * 2);
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.post-card__description {
		font-size: var(--font-size-base);
		color: var(--color-text-secondary);
		line-height: var(--line-height-relaxed);
		margin: 0 0 var(--space-4) 0;
		/* 2行分の固定高さ */
		height: calc(var(--font-size-base) * var(--line-height-relaxed) * 2);
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.post-card__date {
		font-size: var(--font-size-sm);
		font-weight: var(--font-weight-medium);
		color: var(--color-primary-500);
		margin-top: auto;
	}

	/* レスポンシブデザイン */
	@media (min-width: 768px) {
		.hero__container {
			flex-direction: row;
			text-align: left;
		}

		.hero__image {
			width: 150px;
			height: 150px;
		}

		.hero__title {
			font-size: var(--font-size-5xl);
		}
	}

	/* 768px未満：縦方向レイアウト、テキスト中央揃え */
	@media (max-width: 767px) {
		.hero__container {
			text-align: center;
		}

		.hero__text {
			text-align: center;
		}
	}

	/* 480px以下：モバイル最適化 */
	@media (max-width: 480px) {
		.hero {
			padding: var(--space-8) var(--space-4);
			margin: var(--space-2);
		}

		.hero__title {
			font-size: var(--font-size-3xl);
		}

		.hero__description {
			font-size: var(--font-size-base);
		}

		.hero__image {
			width: 80px;
			height: 80px;
		}

		.section-title {
			font-size: var(--font-size-2xl);
		}

		.nav-section,
		.posts-section {
			padding: var(--space-12) var(--space-4);
		}
	}

	/* 320px〜：極小画面対応 */
	@media (max-width: 359px) {
		.hero {
			padding: var(--space-6) var(--space-3);
			margin: var(--space-1);
		}

		.hero__title {
			font-size: var(--font-size-2xl);
		}

		.hero__description {
			font-size: var(--font-size-sm);
		}

		.hero__image {
			width: 64px;
			height: 64px;
		}

		.section-title {
			font-size: var(--font-size-xl);
		}

		.nav-section,
		.posts-section {
			padding: var(--space-8) var(--space-2);
		}
	}

	/* ダークモード */
	html.dark .hero {
		background: linear-gradient(
			135deg,
			var(--color-gray-800) 0%,
			oklch(28% 0.06 var(--primary-hue)) 100%
		);
	}

	html.dark .nav-section {
		background: var(--color-gray-800);
		box-shadow: var(--shadow-md);
	}

	html.dark .posts-section {
		background: var(--color-gray-900);
		box-shadow: var(--shadow-md);
	}
</style>
