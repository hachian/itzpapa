# Implementation Plan

## Tasks

- [x] 1. h2見出しにライトモード用の装飾スタイルを追加する
  - 左ボーダー（4px、アクセントカラー）と薄い背景色を組み合わせた装飾を適用
  - 適切なパディングと角丸を設定し、見出しテキストが読みやすい状態を維持
  - 上部マージンを追加してセクション間の区切りを明確化
  - 既存のフォントサイズ・ウェイト設定は変更しない
  - _Requirements: 1.1, 1.2, 1.3, 4.1, 4.2, 5.1, 5.2_

- [x] 2. h3見出しにライトモード用の装飾スタイルを追加する
  - 左ボーダー（3px、h2より細め）のみを適用し、背景色は使用しない
  - h2より控えめな視覚的ボリュームでサブセクションとしての階層を表現
  - 左パディングでテキストとボーダーの間隔を確保
  - _Requirements: 2.1, 2.2, 2.3, 4.1, 4.2, 5.1, 5.2_

- [x] 3. h4見出しにライトモード用の装飾スタイルを追加する
  - 下線（破線、薄いグレー）のみを適用し、左ボーダーは使用しない
  - h2/h3とは異なる装飾タイプで最も控えめな視覚的ボリュームを実現
  - 下部パディングでテキストと下線の間隔を確保
  - _Requirements: 3.1, 3.2, 3.3, 4.1, 4.2, 5.1, 5.2_

- [x] 4. ダークモード用の見出し装飾スタイルを追加する
  - h2の背景色をダークモード用に調整（より濃いアクセントカラーの透過色）
  - h4の下線色をダークモード用に調整（暗い背景に対して視認性を維持）
  - `html.dark`セレクタと`@media (prefers-color-scheme: dark)`の両方に対応
  - ライトモードと同等の階層区別を維持
  - _Requirements: 1.4, 2.4, 3.4, 4.3_

- [x] 5. モバイル向けレスポンシブスタイルを追加する
  - 768px未満の画面幅でh2のパディングを縮小
  - 768px未満の画面幅でh3の左パディングを縮小
  - コンテンツの可読性を損なわない範囲で調整
  - _Requirements: 6.1, 6.2, 6.3_

- [x] 6. 見出し装飾の視覚確認を実施する
  - ライトモードでh2-h4の装飾が正しく表示されることを確認
  - ダークモードでh2-h4の装飾が正しく表示されることを確認
  - モバイル画面（767px以下）でパディングが適切に縮小されることを確認
  - h2, h3, h4を連続して配置した際に階層が明確に区別できることを確認
  - 既存のブログ記事で見出しスタイルが正しく適用されることを確認
  - _Requirements: 4.3, 5.3_
